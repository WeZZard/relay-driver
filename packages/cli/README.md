# @wezzard/relay-driver-cli

Command-line interface for Relay Driver: inspect sessions and executions, verify packages, and serve the review viewer.

Install with `npm install -g @wezzard/relay-driver-cli`. See the [root README](https://github.com/WeZZard/relay-driver#readme) for the full project.

MIT licensed.
